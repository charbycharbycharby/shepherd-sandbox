# Shepherd Sandbox — Pong

This repository contains a minimal Vite + React + TypeScript app with a simple Pong game.

Commands:

- npm install
- npm run dev

Controls:
- Left paddle: W / S
- Right paddle: Arrow Up / Arrow Down (also simple AI when not controlled)
- Space: start / resume
